#### to run cypress
      npx cypress open
###### for  input value  
cy.contains

##### for visit page on every test scenario
    beforeEach(() => {
        cy.visit('url');
    });
Direct Access 
through hook
